[project]
name = "document-assistant-server"
version = "0.1.0"
description = "Backend API for the Document Assistant mobile app"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
